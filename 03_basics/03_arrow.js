// Traditional function expression
const add1 = function(a, b) {
    return a + b;
  };
  
  // Arrow function expression
  const add2 = (a, b) => {
    return a + b;
  };
  
  // Even shorter version (when there's only a single expression)
  const add3 = (a, b) => a + b;
  